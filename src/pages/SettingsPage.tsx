import React, { useEffect, useState } from 'react';
import { CheckCircle2, Cloud, UserPlus } from 'lucide-react';
import { api, pingApi } from '../lib/api';

type UserRow={id:string;username:string;fullName:string;role:string;status:string};
export function SettingsPage(){const[ok,setOk]=useState<boolean|null>(null);const[users,setUsers]=useState<UserRow[]>([]);const[form,setForm]=useState({username:'',fullName:'',role:'Viewer',password:''});const[msg,setMsg]=useState<string|null>(null);const load=()=>api<UserRow[]>('listUsers').then(setUsers).catch(()=>{});useEffect(()=>{load()},[]);const test=async()=>setOk(await pingApi());const save=async()=>{try{await api('saveUser',{user:form});setForm({username:'',fullName:'',role:'Viewer',password:''});setMsg('تم إنشاء المستخدم.');load()}catch(e){setMsg(e instanceof Error?e.message:'تعذر الإنشاء')}};return <><div className="page-header"><div><h1>إعدادات النظام</h1><p>إدارة اتصال الخادم وحسابات موظفي النظام. لا يتم عرض كلمات المرور المخزنة.</p></div></div><div className="grid grid-2"><div className="card panel"><div className="panel-header"><h3><Cloud size={16}/> حالة الخادم</h3></div><button className="btn btn-light" onClick={test}>اختبار الاتصال</button>{ok!==null&&<div className={`alert ${ok?'alert-success':'alert-danger'}`} style={{marginTop:12}}>{ok?<><CheckCircle2 size={14}/> الخادم متصل</>:'الخادم غير متاح'}</div>}</div><div className="card panel"><div className="panel-header"><h3><UserPlus size={16}/> إضافة مستخدم موظف</h3></div><div className="form-grid"><input className="input" placeholder="اسم المستخدم" value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/><input className="input" placeholder="الاسم الكامل" value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/><select className="select" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>Admin</option><option>SchoolDirector</option><option>StudentAffairs</option><option>TeacherAffairs</option><option>HR</option><option>SocialSpecialist</option><option>QualityOfficer</option><option>Viewer</option></select><input className="input" type="password" placeholder="كلمة مرور مؤقتة قوية" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button className="btn btn-primary" onClick={save}>إنشاء المستخدم</button>{msg&&<div className="small muted">{msg}</div>}</div></div></div><div className="card panel" style={{marginTop:16}}><div className="panel-header"><h3>مستخدمو النظام</h3></div><div className="table-wrap"><table><thead><tr><th>المستخدم</th><th>الاسم</th><th>الدور</th><th>الحالة</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.username}</td><td>{u.fullName}</td><td>{u.role}</td><td>{u.status}</td></tr>)}</tbody></table></div></div></>}
