import React,{useEffect,useState}from'react';import{api}from'../lib/api';import type{AuditLog}from'../types';export function AuditPage(){const[rows,setRows]=useState<AuditLog[]>([]);useEffect(()=>{api<AuditLog[]>('listAuditLogs').then(setRows)},[]);return <><div className="page-header"><div><h1>سجل التدقيق</h1><p>تاريخ العمليات الحساسة كما سجلها الخادم.</p></div></div><div className="card panel"><div className="table-wrap"><table><thead><tr><th>الوقت</th><th>المستخدم</th><th>العملية</th><th>الكيان</th><th>التفاصيل</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.timestamp}</td><td>{r.actorId}<div className="small muted">{r.actorRole}</div></td><td>{r.action}</td><td>{r.entity}</td><td>{r.details||r.entityId}</td></tr>)}</tbody></table></div></div></>}
